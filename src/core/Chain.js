// Chain operations.

$.chain = function(how, ops, callback){
  
};

$.chain.paralell = function(ops, callback){
  
};

// Wait for first to go to next.
$.chain.linear = function(ops, callback) {
  
};